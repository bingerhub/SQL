Superstore Database questions and code for answers

--Write a SQL query that orders the items by price

SELECT (price) item_name, category
FROM superstore
WHERE category = "Kitchen Supplies"
ORDER BY price;

--total item in stock

SELECT stock_quantity
FROM superstore
WHERE item_name ="Air Purifier";

